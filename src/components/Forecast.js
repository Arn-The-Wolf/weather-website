import React from 'react';

const Forecast = ({ data }) => {
    return (
        <div className="forecast">
            {/* Render forecast data */}
        </div>
    );
};

export default Forecast;