import React from 'react';

const WeatherItem = ({ item }) => {
    return (
        <div className="weather-item">
            {/* Render individual weather item */}
        </div>
    );
};

export default WeatherItem;